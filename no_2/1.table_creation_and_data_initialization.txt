--CREATE ACCOUNT TABLE
CREATE TABLE ACCOUNT(ID NUMBER(10),
NAME VARCHAR2(20), ACCOUNTNO 
VARCHAR2(20), ORIGINALBALANCE NUMBER(30));

--INSERT DATA INTO ACCOUNT TABLE
INSERT ALL
    INTO ACCOUNT VALUES (1,'Budi','12345654321',10000000)
    INTO ACCOUNT VALUES (2,'Ani','56789098765',12000000)
SELECT 1 FROM DUAL;

--DATA VERIFICATION QUERY
SELECT * FROM ACCOUNT;

-- COMMIT IF OK
COMMIT;

-- CREATE HOLDBALANCE TABLE
CREATE TABLE HOLDBALANCE (ID NUMBER(10),
ACCOUNTNO VARCHAR2(20), HOLDBALANCE NUMBER(30));

-- INSERT DATA INTO HOLDBALANCE TABLE
INSERT ALL
    INTO HOLDBALANCE VALUES (1,'12345654321',500000)
    INTO HOLDBALANCE VALUES (2,'56789098765',800000)
SELECT 1 FROM DUAL; 

--DATA VERIFICATION QUERY
SELECT * FROM HOLDBALANCE;

-- COMMIT IF OK
COMMIT;

DROP TABLE MASTERACCOUNT;

-- CREATE MASTER ACCOUNT TABLE
CREATE TABLE MASTERACCOUNT (ID NUMBER(10),
NAME VARCHAR(20), ACCOUNTNO VARCHAR(20),
AVAILABLEBALANCE NUMBER(30));

SELECT * FROM MASTERACCOUNT;

-- EXECUTE AFTER PROCEDURE COMPILED
EXEC MIG_ACCOUNT_TAB;