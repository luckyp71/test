1. Create Table CUSTOMER_V2
CREATE TABLE CUSTOMER_V2 (id number(20), NAME VARCHAR(50), PHONE VARCHAR(30));

2. Invoke services with the sample payloads given in the src/main/resources/sample_payloads.txt